name: open_dspc
description: ⚡ Dart/Flutter DSP package with a lightweight native C backend
version: 0.0.1
homepage: https://github.com/nipponjo/open-dspc-flutter

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  code_assets: ^1.0.0
  hooks: ^1.0.1
  native_toolchain_c: ^0.17.4
  logging: ^1.3.0  
  ffi: ^2.2.0

dev_dependencies:
  ffigen: ^20.1.1
  flutter_lints: ^6.0.0
  test: ^1.30.0

topics:
  - dsp
  - signal
  - fft
  - flutter
  - ffi
